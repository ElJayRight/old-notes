The name of the service that kerberos identifies them as.
Each computer has its own SPN
So active directory will give it a TGT as it is just acting as normal.

Format

serviceclasslhost:port service name
